macro(InstallMyUgens PROYECTO)
	if (NOT APPLE)
		install(TARGETS ${PROYECTO} DESTINATION ${INSTALL_DESTINATION})
		if(SUPERNOVA)
			install(TARGETS ${PROYECTO}_supernova DESTINATION ${INSTALL_DESTINATION})
		endif()
	else()
		install(TARGETS ${PROYECTO} DESTINATION ${INSTALL_DESTINATION}/${PROYECTO})
		if(SUPERNOVA)
			install(TARGETS ${PROYECTO}_supernova DESTINATION ${INSTALL_DESTINATION}/${PROYECTO})
		endif()
	endif()
	
	if(EXISTS ${CMAKE_SOURCE_DIR}/${PROYECTO}/sc/)
		install(DIRECTORY ${CMAKE_SOURCE_DIR}/${PROYECTO}/sc/ DESTINATION ${INSTALL_DESTINATION_DISTRO}/${PROYECTO} PATTERN "*")
	endif()
	install(FILES ${CMAKE_SOURCE_DIR}/${PROYECTO}/lua/${PROYECTO}.lua DESTINATION ${INSTALL_DESTINATION_DISTRO}/lua )
endmacro()

